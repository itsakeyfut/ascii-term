[package]
name = "codec"
version = "0.1.0"
edition = "2024"

[dependencies]
# メディアデコード
avio = { version = "0.14.3", features = ["tokio"] }

# 画像処理
image = { workspace = true }

# 非同期処理
tokio = { workspace = true }

# エラーハンドリング
thiserror = { workspace = true }

